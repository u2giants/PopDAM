# === Tailscale ===
TAILSCALE_AUTH_KEY=tskey-auth-xxxxx

# === DAM API ===
# Your Lovable Cloud edge function base URL
SUPABASE_URL=https://onvjizdwbxayvyneomey.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# === Agent Identity ===
AGENT_NAME=edgesynology2
AGENT_KEY=bridge-agent-edgesynology2

# === Scan Configuration ===
# Comma-separated root folders to scan (inside the container mount)
SCAN_ROOTS=/mnt/nas/Design
# Only process files modified after this date (ISO 8601). Leave empty to scan all.
SCAN_MIN_DATE=2020-01-01
# Scan interval in minutes
SCAN_INTERVAL_MINUTES=10
# File extensions to process (comma-separated, lowercase, no dot)
SCAN_EXTENSIONS=psd,ai

# === Thumbnails ===
# Max thumbnail dimension (longest side)
THUMBNAIL_MAX_SIZE=800
# JPEG quality (1-100)
THUMBNAIL_QUALITY=85

# === DigitalOcean Spaces (S3-compatible) ===
DO_SPACES_KEY=your-spaces-access-key
DO_SPACES_SECRET=your-spaces-secret-key
DO_SPACES_REGION=nyc3
DO_SPACES_BUCKET=popdam
