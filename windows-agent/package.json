{
  "name": "dam-windows-render-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Windows Render Agent — uses Adobe Illustrator to generate thumbnails for AI files without PDF compatibility",
  "scripts": {
    "build": "tsc",
    "start": "node -r dotenv/config dist/index.js",
    "dev": "npx tsx src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "sharp": "^0.33.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0"
  }
}
